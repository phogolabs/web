---
platform: linux

image_resource:
  type: docker-image
  source: {repository: phogo/ruby, tag: latest}

inputs:
- name: git-repository

outputs:
- name: version

run:
  path: repository/ci/script/version.sh
